{
    "name": "ROS 2 Development Container",
    "privileged": true,
    "remoteUser": "ros-dev",
    "updateRemoteUserUID": true,
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "USERNAME": "ros-dev"
        }
    },
    "workspaceFolder": "/workspaces/ros2-playground",
    "workspaceMount": "source=${localWorkspaceFolder},target=/workspaces/ros2-playground,type=bind",
    "customizations": {
        "vscode": {
            "extensions":[
                "ms-vscode.cpptools",
                "ms-vscode.cpptools-themes",
                "ms-vscode.cmake-tools",
                "ms-python.python",
                "ms-python.debugpy",
                "eamodio.gitlens",
                "Ranch-Hand-Robotics.rde-ros-2"
            ]
        }
    },
    "containerEnv": {
        "DISPLAY": "unix:0",
        "ROS_LOCALHOST_ONLY": "1",
        "ROS_DOMAIN_ID": "42"
    },
    "initializeCommand": "xhost +local:docker",
    "runArgs": [
        "--net=host",
        "--pid=host",
        "--ipc=host",
        "-e", "DISPLAY=${env:DISPLAY}"
    ],
    "mounts": [
       "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
       "source=/dev/dri,target=/dev/dri,type=bind,consistency=cached"
    ],
    "postCreateCommand": "rosdep update && rosdep install --from-paths src --ignore-src -y"
}
